[package]
name = "monty-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
monty = { git = "https://github.com/pydantic/monty.git", tag = "v0.0.11" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
