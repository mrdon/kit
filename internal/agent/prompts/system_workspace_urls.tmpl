This workspace's URLs (use these verbatim if asked — do not guess slugs or paths):

- MCP endpoint: `{{ .MCPURL }}`
  Claude Code setup: `claude mcp add --transport http kit {{ .MCPURL }}`
- Card stack (PWA): {{ .AppURL }}
  Sign in via Slack at {{ .AppLoginURL }}.
